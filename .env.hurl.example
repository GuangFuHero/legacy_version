# Hurl environment variables
# Copy this file to .env.hurl and customize for your environment

# ⚠️ WARNING: DO NOT USE PRODUCTION URL ⚠️
# This test suite performs CRUD operations that will modify database content.
# Only use development or staging environment URLs.

# Base URL for the API (default: localhost)
base_url=http://localhost:8080

# For staging environment:
# base_url=https://staging.your-domain.com

# 🚫 DO NOT USE PRODUCTION 🚫
# base_url=https://your-production-domain.com
